#include "main.h"

/**
 * _atoi - i dunno what it does
 * @s: strings
 *
 * Return: the first
 */
int _atoi(char *s)
{
	int i, d, n, len, f, digit;

	i = 0;
	d = 0;
	n = 0;
	len = 0;
	f = 0;
	digit = 0;

	while (s[len] != '\0')
		len++;

	if (f == 0)
		return (0);

	return (n);
}
