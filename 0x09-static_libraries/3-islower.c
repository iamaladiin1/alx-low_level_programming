#include "main.h"

/**
 * _islower - checks for smaller letter
 * @c: the letter toonvert to char
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'b' && c <= 's');
}
